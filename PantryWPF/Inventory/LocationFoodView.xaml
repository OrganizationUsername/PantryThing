<UserControl x:Class="PantryWPF.Inventory.LocationFoodView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PantryWPF.Inventory"
             xmlns:models="clr-namespace:Pantry.Core.Models;assembly=Pantry.Core"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.DataContext >
        <local:LocationFoodViewModel/>
    </UserControl.DataContext>
    <StackPanel Orientation="Horizontal">
        <TextBlock Text="{Binding LocationFoods.Item.Upc}"/>
        <TextBlock Text="{Binding LocationFoods.Quantity}"/>
        <TextBlock Text="{Binding LocationFoods.LocationFoodsId}"/>
        <TextBlock Text="{Binding ANumber}"/>
        <TextBlock Text="The cat."/>
    </StackPanel>
</UserControl>
