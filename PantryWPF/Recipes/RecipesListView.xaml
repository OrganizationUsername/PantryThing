<UserControl x:Class="PantryWPF.Recipes.RecipesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PantryWPF.Inventory"
             xmlns:recipes="clr-namespace:PantryWPF.Recipes"
             xmlns:models="clr-namespace:Pantry.Core.Models;assembly=Pantry.Core"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.DataContext >
        <recipes:RecipesListViewModel />
    </UserControl.DataContext>
    <UserControl.Resources>
        <DataTemplate DataType="{x:Type models:Recipe}">
            <TextBlock Text="{Binding MainOutput.Name}"/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type recipes:RecipeDetailViewModel}">
            <recipes:RecipeDetailView />
        </DataTemplate>
    </UserControl.Resources>
    <DockPanel>
        <ListView ItemsSource="{Binding ACollection}" DockPanel.Dock="Left" 
                  SelectedItem="{Binding SelectedRecipe}"/>
        <ContentControl Content="{Binding SelectedRecipeDetailViewModel}" DockPanel.Dock="Right"/>
    </DockPanel>
</UserControl>
